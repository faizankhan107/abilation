{
  "batch_size": 16,
  "num_epochs": 50,
  "learning_rate": 4e-5,
  "weight_decay": 1e-4,
  "patience": 10,
  "min_delta": 1e-4,
  "image_size": [256, 256],
  "num_workers": 4,
  "pin_memory": true,
  "mixed_precision": true,
  "gradient_clip_value": 1.0,
  "save_every_n_epochs": 5,
  
  "comments": {
    "batch_size": "Adjust based on GPU memory (8-32 typical range)",
    "num_epochs": "Maximum number of training epochs",
    "learning_rate": "Starting learning rate (4e-5 works well for most models)",
    "patience": "Early stopping patience (epochs without improvement)",
    "image_size": "Input image size [height, width] - 256x256 recommended",
    "mixed_precision": "Enable automatic mixed precision for faster training",
    "gradient_clip_value": "Gradient clipping value to prevent exploding gradients"
  }
}